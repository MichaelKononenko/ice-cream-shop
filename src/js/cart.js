const refs = {
  cartIsVisible: document.querySelector('.header__bascket-icon'),
};
let qty = document.getElementById('firstCount');
qty = qty.innerHTML;
// qty = Number.qty;
// if (qty.innerHTML < 1) {
//   refs.cartIsVisible.classList.add('is-hiden');
// }
console.log(`qty = ${qty.value}`);
document.getElementById('qty').value = this.innerHTML;
